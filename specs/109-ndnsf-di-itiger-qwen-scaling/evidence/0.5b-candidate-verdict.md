# 0.5B NDNSF-DI candidate verdict

## Verdict

`BLOCKED` by the exact predecessor gate. No NDNSF-DI GPU candidate job was
submitted, so no candidate correctness, performance, overhead, or physical
production claim exists.

The gate observation in `results/spec109-itiger-qwen/predecessor-gate.json`
binds all required Spec 107 T027/T028–T038 and Spec 108 T091–T102 entries. All
24 entries remain `INCOMPLETE`; its immutable observation digest is
`sha256:57467636a7ff01408847e7c850ebc1f5e002afd05ffe8efa68776103adea054d`.

## What was completed

- Parameterized model repository, immutable revision, dtype, stage count,
  execution provider, and logical-GPU mapping replaced the 0.5B/CPU-only native
  manifest assumptions.
- Float16 hidden/KV interoperability was verified without changing the existing
  supported codec behavior.
- ONNX Runtime profiling can now retain every model-node provider assignment,
  GPU UUID, fallback state, runtime version, stage roles, and artifact digests.
- Allocation-local NFD/controller/provider/user orchestration, bounded readiness,
  trap cleanup, one-node multi-GPU Slurm rendering, and candidate CLI operations
  were added.
- Focused implementation checks passed: 9 C++ tests, 10 Spec 109 Python tests,
  2 Python binding regressions after rebuild, and the packaged security
  contract. The MiniNDN fake LLM pipeline still failed with `local deadline`;
  that failure is retained separately and does not become GPU evidence.

## Terminal cells

The 15-cell 0.5B matrix in
`results/spec109-itiger-qwen/candidates/0.5B-ndnsf-di.json` is finalized with
every cell `BLOCKED`, no run IDs, and no evidence digests. Per-output records
under `artifacts/0.5B/` and `ndnsf-di/0.5B/` preserve T091–T104 as not submitted.
No failed measurement was retried or converted into a pass.

Standalone source-model staging remains PASS. Candidate and physical-production
authority remain respectively `BLOCKED` and `DEFERRED`.
