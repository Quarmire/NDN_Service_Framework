# 0.5B standalone reference verdict

## Verdict

`BLOCKED` — the standalone GPU reference is not available and does not confer
NDNSF-DI or physical-production authority.

The sealed Qwen2.5-0.5B-Instruct source model is valid and remains protected in
project storage. However, Spec 108 tasks T091–T102 have no accepted GPU release,
OCI/SIF lineage, or deployment digest. Spec 109 therefore cannot materialize or
run the Qwen experiment image without violating its predecessor boundary.

## Terminal closure

- T066: `BLOCKED` by `SPEC108_GPU_PREDECESSOR_INCOMPLETE`.
- T067: workload and decoding choices are frozen in
  `experiment/0.5b-reference.json`, but all deployment digests remain null and
  the profile is explicitly non-accepted.
- T068–T069: the diagnostic and acceptance-candidate records are terminally
  blocked; no diagnostic job was submitted.
- T070–T074: the 1/2/32-token oracle, reconciliation, and resource evidence are
  terminally blocked; no oracle job was submitted and no token or performance
  claim is made.
- T075: this document records the mechanical verdict.

The controlling evidence is
`results/spec109-itiger-qwen/releases/gpu/predecessor-block.json`, bound to gate
digest `sha256:28feac865252229b66ea0761b805ba76107ceacd466fb42769d32e99f6889ba7`.
Dependent records live under `candidates/0.5B-reference.json`,
`diagnostics/0.5B-standalone/blocked.json`, and `reference/0.5B/blocked.json`.
Every record states `jobSubmitted=false`.

## Preserved result

The outcome is a systemic predecessor failure, not a model failure. The staged
0.5B source registry and storage-policy PASS remain admissible. No measured cell
was retried, replaced, or silently skipped. Physical deployment remains under
Spec 106 and is `DEFERRED`.
