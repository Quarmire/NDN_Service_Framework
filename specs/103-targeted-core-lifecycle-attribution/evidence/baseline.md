# Baseline
Core TRACE already contains `TARGETED_REQUEST_ACCEPTED`, `PROVIDER_EXECUTE_DONE`,
`RESPONSE_PUBLISH_ATTEMPT`, `RESPONSE_PUBLISHED`, and `RESPONSE_PUBLISH_FAILED`.
Campaign default ServiceProvider WARN hides them. No core change is necessary.
