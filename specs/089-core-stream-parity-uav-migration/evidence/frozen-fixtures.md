# Frozen Fixtures

Parity vectors cover session reset, out-of-order `1,0`, duplicate replay,
stale epoch, gap `2,3`, skip, bounded overflow, missing range, metrics, RTT,
timeout/NACK/duplicate/backlog pressure and decay. UAV fixtures preserve exact
video wire, FEC recovery inputs, adaptive state, authority and mission behavior.
